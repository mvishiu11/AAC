[project]
name = "benchmark-plotter"
version = "0.1.0"
description = "Plotting scripts for AAC benchmark"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pandas",
    "matplotlib",
    "seaborn",
    "networkx",
    "jupyter",
    "ipykernel>=6.29.5",
]
